(ns ylserver.routes.old
  (:require [ylserver.layout :as layout]
            [compojure.core :refer :all]
            [ring.util.http-response :refer [ok]]
            [clojure.java.io :as io]
            [ylserver.controller.old :as old]))



(defroutes old-routes
;  (GET "/test2" (layout/render "about.html"))
;  (POST "/addold" [oldname oldcardnum oldtelnum hometown oldaddress range equipmentnum dvcode contactname1 contacttel1 contactname2 contacttel2 oldpwd]
;    (old/addold oldname oldcardnum oldtelnum hometown oldaddress range equipmentnum dvcode contactname1 contacttel1 contactname2 contacttel2 oldpwd))
;  (GET "/addhealthy" [hname hcontent oldid] (old/addhealthy hname hcontent oldid))
  )

